package Game;

// Multidoku pouvant gérer des blocs dispersés
public class Multidoku extends Sudoku {

    public Multidoku(int taille) {
        super(taille);
    }

    @Override
    public void generer(Difficulte difficulte) {
        // Redéfinir la méthode generer
    }
}
